/**
 * **Last deployed** line in the global footer.
 *
 * Update this string when you **deploy** or ship a notable UI/backend change — and add a
 * matching row in **`DEVLOG.md`** (Sessions table) so the repo stays the source of truth.
 */
export const SITE_LAST_DEPLOYED =
  "24 Mar 2026, 02:00 IST";

/** ISO instant for the `<time>` element (match the deploy you document in DEVLOG). */
export const SITE_LAST_DEPLOYED_ISO = "2026-03-23T20:30:00.000Z";
